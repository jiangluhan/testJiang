<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.google.authenticator.dao.GoogleAuthDao">
    <resultMap id="BaseResultMap" type="com.google.authenticator.po.GoogleAuthPo">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="user_name" jdbcType="VARCHAR" property="userName" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="secret_key" jdbcType="VARCHAR" property="secretKey" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
    </resultMap>
    
    <sql id="Base_Column_List">
        id, user_name, password, secret_key, create_time, update_time
    </sql>

    <insert id="initUser" parameterType="com.google.authenticator.po.GoogleAuthPo">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into user (user_name, password, secret_key, create_time, update_time)
        value (#{userName}, #{password}, #{secretKey}, NOW(), NOW())
    </insert>

    <select id="getUserById" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"></include>
        from user
        where id = #{userId}
    </select>

    <select id="getUserList" resultType="com.google.authenticator.po.GoogleAuthPo">
        select <include refid="Base_Column_List"></include>
        from user
    </select>
</mapper>